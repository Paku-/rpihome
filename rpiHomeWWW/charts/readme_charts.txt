Charts and charts folder is separated from main GUI.
It's made so as it uses a lot of javascript libraries 
not so important for main GUI.

Library used is jqwidgets (jQuery included).

Library ZIP file added for archive only.
You may delete for production.